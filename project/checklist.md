# Checklist

- Existe una CLI ejecutable
- Existe un `worker` local
- Existen prompts por agente
- Existe memoria externa en `project/`
- Existen logs de ejecucion
- Existe proveedor `mock`
- Existe proveedor `openai-compatible`
- Existe soporte de `workspace` local
- Existen permisos basicos para tools locales
- La documentacion explica como usar la v1
